# REVENANT backend — RUNTIME only. Python 3.13.
#
# Test and audit tooling lives in requirements-dev.txt: a production image has
# no business carrying a test runner, and pip-audit's pins conflict with
# pytest's, which broke the Docker build.
#
# Versions pinned after a pip-audit sweep — fastapi, starlette, python-dotenv
# and pytest all carried known CVEs at their original pins.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.4
asyncpg==0.30.0
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.2.3
httpx==0.28.1

# Razorpay test-mode integration
razorpay==2.0.1
# razorpay 2.0.1 still imports pkg_resources at runtime, absent by default on
# Python 3.12+. Declared so installs are deterministic rather than lucky.
setuptools>=75.0.0

# Agents (D4 revised: Gemini free tier, Anthropic selectable)
langgraph==1.2.11
langchain-google-genai==4.3.6
langchain-anthropic==1.6.1
