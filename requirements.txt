# REVENANT backend. Python 3.13.
# Versions pinned after a pip-audit sweep: fastapi/starlette/python-dotenv/pytest
# all carried known CVEs at the originally pinned versions.
fastapi==0.141.1
starlette==1.6.0
uvicorn[standard]==0.52.4
asyncpg==0.30.0
pydantic==2.13.4
pydantic-settings==2.7.1
python-dotenv==1.2.3
httpx==0.28.1

# Razorpay test-mode integration
razorpay==2.0.1
# razorpay 2.0.1 still imports pkg_resources at runtime, which is not present
# by default on Python 3.12+. Declared explicitly so installs are deterministic.
setuptools>=75.0.0

# Agents (decision D4 revised: Gemini free tier, Anthropic selectable)
langgraph==1.2.11
langchain-google-genai==4.3.6
langchain-anthropic==1.6.1

# Dev / test
pytest==9.1.1
pytest-asyncio==0.25.2
pip-audit>=2.7.0
